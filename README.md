# Image Paper Hub｜图像相关论文收集

一个可直接部署到 GitHub Pages 的静态论文收录网站，结构参考会议论文清单 README。

## 功能

- 总论文数、大类数、小类数、arXiv、代码统计
- 多级分类：大类 + 小类
- 按章节展示论文列表
- 分类、年份、关键词筛选
- 结构化数据文件 `papers.json`
- GitHub Actions 每日自动检索 arXiv 并更新

## 本地预览

```bash
python -m http.server 8000
```

然后访问：`http://localhost:8000`

## 添加论文

编辑 `papers.json` 的 `papers` 数组，新增一条记录即可。

## 自动检索更新

项目内置每日自动检索脚本：

```bash
python scripts/update_papers.py --max-results 3
```

它会从 arXiv 检索最近的图像相关论文，按方向合并到 `papers.json`，并自动跳过已经存在的标题或 arXiv ID。

## GitHub 每日自动更新

`.github/workflows/daily-update.yml` 已配置为每天北京时间 09:00 自动运行一次，并在 `papers.json` 有变化时自动提交。

也可以在 GitHub 仓库页面手动运行：

`Actions` → `Daily Paper Update` → `Run workflow`

## ???????

???????? arXiv??? GitHub Actions ????? 2025?2026 ?????????

- OpenAlex???????????????????
- arXiv???????????
- Semantic Scholar???????????????????????????

???????????? `scripts/update_papers.py` ? `SEARCH_PROFILES` ?????????
